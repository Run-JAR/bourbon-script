// test_fibonacci.bon

recipe fib(n: int) -> int:
    if n <= 1:
        plate n
    otherwise:
        plate fib(n - 1) + fib(n - 2)

recipe main() -> void:
    bake i from 0 to 10:
        display(fib(i))
